This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
backend/
  prisma/
    migrations/
      20260607215537_init_usuarios/
        migration.sql
      migration_lock.toml
    schema.prisma
    seed.js
  src/
    controllers/
      auth.controller.js
    middlewares/
      auth.middleware.js
    routes/
      auth.routes.js
    app.js
    server.js
  .gitignore
  package.json
docs/
  setup-inicial.md
frontend/
  index.html
  server.js
.gitignore
README.md
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".gitignore">
repomix-output.txt
</file>

<file path="backend/prisma/migrations/20260607215537_init_usuarios/migration.sql">
-- CreateEnum
CREATE TYPE "Rol" AS ENUM ('ADMIN', 'ODONTOLOGO', 'RECEPCIONISTA');

-- CreateTable
CREATE TABLE "Usuario" (
    "id" SERIAL NOT NULL,
    "nombre" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "rol" "Rol" NOT NULL DEFAULT 'RECEPCIONISTA',
    "activo" BOOLEAN NOT NULL DEFAULT true,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "Usuario_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "Usuario_email_key" ON "Usuario"("email");
</file>

<file path="backend/prisma/migrations/migration_lock.toml">
# Please do not edit this file manually
# It should be added in your version-control system (e.g., Git)
provider = "postgresql"
</file>

<file path="backend/prisma/seed.js">
const { PrismaClient } = require('@prisma/client')
const bcrypt = require('bcryptjs')

const prisma = new PrismaClient()

async function main() {
  const hash = await bcrypt.hash('admin123', 10)

  await prisma.usuario.upsert({
    where: { email: 'admin@odontosoft.com' },
    update: {},
    create: {
      nombre: 'Administrador',
      email: 'admin@odontosoft.com',
      password: hash,
      rol: 'ADMIN'
    }
  })

  console.log('✅ Usuario admin creado')
}

main()
  .catch(console.error)
  .finally(() => prisma.$disconnect())
</file>

<file path="backend/.gitignore">
# Dependencias de Node
node_modules/

# Archivos de entorno local (¡Crítico para seguridad!)
.env

# Archivos de logs de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de sistema y editores
.DS_Store
.vscode/

# Si decides generar documentación automática o builds en el futuro
dist/
build/
coverage/

# Repomix
repomix-output.txt
</file>

<file path="docs/setup-inicial.md">
# Configuración Inicial del Proyecto OdontoSoft

Documentación del estado actual del sistema y la configuración del entorno.

## 1. Estructura del Proyecto
El proyecto está organizado para separar la lógica del servidor de la interfaz:
- `/backend`: Servidor Node.js con Express.
- `/frontend`: Interfaz de usuario (Vanilla JS).
- `/docs`: Documentación técnica.

## 2. Tecnologías y Entorno
- **Plataforma**: GitHub Codespaces.
- **Runtime**: Node.js v24+.
- **Framework Backend**: Express.js.
- **Herramientas de Desarrollo**: Nodemon (recarga automática).

## 3. Configuración de Seguridad (.gitignore)
Se han implementado archivos `.gitignore` en la raíz, `/backend` y `/frontend` para:
- Excluir `node_modules/` (dependencias pesadas).
- Proteger archivos de variables de entorno `.env` (credenciales de DB).
- Ignorar archivos de configuración de VS Code y logs del sistema.

## 4. Estado del Backend
- Servidor inicializado con `npm init`.
- Dependencias instaladas: `express`, `mongoose`, `dotenv`, `cors`, etc.
- Punto de entrada: `server.js`.
- Endpoints activos: 
  - `GET /` (Bienvenida).
  - `GET /api/health` (Estado del servidor).

---
*Nota: Este documento se actualizará conforme se avance en la conexión a la base de datos y la implementación de modelos.*
</file>

<file path="backend/prisma/schema.prisma">
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario {
  id        Int      @id @default(autoincrement())
  nombre    String
  email     String   @unique
  password  String
  rol       Rol      @default(RECEPCIONISTA)
  activo    Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum Rol {
  ADMIN
  ODONTOLOGO
  RECEPCIONISTA
}
</file>

<file path="backend/src/controllers/auth.controller.js">
const { PrismaClient } = require('@prisma/client')
const bcrypt = require('bcryptjs')
const jwt = require('jsonwebtoken')

const prisma = new PrismaClient()

const login = async (req, res) => {
  try {
    const { email, password } = req.body

    // 1. Buscar usuario
    const usuario = await prisma.usuario.findUnique({ where: { email } })
    if (!usuario || !usuario.activo) {
      return res.status(401).json({ message: 'Credenciales inválidas' })
    }

    // 2. Verificar password
    const passwordValido = await bcrypt.compare(password, usuario.password)
    if (!passwordValido) {
      return res.status(401).json({ message: 'Credenciales inválidas' })
    }

    // 3. Generar token
    const token = jwt.sign(
      { id: usuario.id, rol: usuario.rol },
      process.env.JWT_SECRET,
      { expiresIn: process.env.JWT_EXPIRES_IN }
    )

    res.json({
      token,
      usuario: {
        id: usuario.id,
        nombre: usuario.nombre,
        email: usuario.email,
        rol: usuario.rol
      }
    })
  } catch (error) {
    console.error(error)
    res.status(500).json({ message: 'Error interno del servidor' })
  }
}

const me = async (req, res) => {
  res.json(req.usuario)
}

module.exports = { login, me }
</file>

<file path="backend/src/middlewares/auth.middleware.js">
const jwt = require('jsonwebtoken')

const verificarToken = (req, res, next) => {
  const authHeader = req.headers['authorization']
  const token = authHeader && authHeader.split(' ')[1] // Bearer TOKEN

  if (!token) {
    return res.status(401).json({ message: 'Token requerido' })
  }

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET)
    req.usuario = decoded
    next()
  } catch (error) {
    return res.status(403).json({ message: 'Token inválido o expirado' })
  }
}

module.exports = { verificarToken }
</file>

<file path="backend/src/routes/auth.routes.js">
const { Router } = require('express')
const { login, me } = require('../controllers/auth.controller')
const { verificarToken } = require('../middlewares/auth.middleware')

const router = Router()

router.post('/login', login)
router.get('/me', verificarToken, me)

module.exports = router
</file>

<file path="backend/src/app.js">
const express = require('express')
const cors = require('cors')
const helmet = require('helmet')
const authRoutes = require('./routes/auth.routes')

const app = express()

app.use(helmet())
app.use(cors({ origin: 'http://localhost:5173' }))
app.use(express.json())

app.use('/api/auth', authRoutes)

app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', app: 'OdontoSoft' })
})

module.exports = app
</file>

<file path="backend/src/server.js">
require('dotenv').config()
const app = require('./app')

const PORT = process.env.PORT || 3001

app.listen(PORT, () => {
  console.log(`🦷 OdontoSoft backend corriendo en puerto ${PORT}`)
})
</file>

<file path="backend/package.json">
{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "seed": "node prisma/seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.3",
    "prisma": "^5.22.0",
    "zod": "^4.4.3"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
</file>

<file path="frontend/server.js">
const http = require('http');
const fs = require('fs');
const path = require('path');

const PORT = 5173;

const server = http.createServer((req, res) => {
    // Servir el archivo index.html
    if (req.url === '/' || req.url === '') {
        const filePath = path.join(__dirname, 'index.html');
        fs.readFile(filePath, 'utf8', (err, data) => {
            if (err) {
                res.writeHead(500, { 'Content-Type': 'text/plain' });
                res.end('Error al cargar el archivo');
                return;
            }
            res.writeHead(200, { 'Content-Type': 'text/html' });
            res.end(data);
        });
    } else {
        res.writeHead(404, { 'Content-Type': 'text/plain' });
        res.end('No encontrado');
    }
});

server.listen(PORT, () => {
    console.log(`Frontend corriendo en: http://localhost:${PORT}`);
});
</file>

<file path="README.md">
# odontosoft2
Navega a la carpeta frontend 
cd frontend
Ejecuta:
python3 -m http.server 3000
Luego abre en el navegador:
http://localhost:3000/index.html
</file>

<file path="frontend/index.html">
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prueba Frontend - Backend</title>
    <style>
        body { font-family: sans-serif; padding: 20px; background: #0f172a; color: white; }
        .card { background: #1e293b; padding: 20px; border-radius: 8px; max-width: 400px; }
        #mensaje { margin-top: 15px; font-weight: bold; color: #38bdf8; }
    </style>
</head>
<body>

    <div class="card">
        <h2>Estado de la API</h2>
        <button id="btn-conectar">Probar Conexión</button>
        <div id="mensaje">Esperando conexión...</div>
    </div>

    <script>
        async function conectarAPI() {
            const mensajeDiv = document.getElementById('mensaje');
            mensajeDiv.innerText = "Cargando...";

            try {
                const urlBackend = 'https://cautious-space-dollop-6447jx4x47xf546j-3000.app.github.dev/api/health';
                
                const respuesta = await fetch(urlBackend);
                const datos = await respuesta.json();
                
                mensajeDiv.innerText = datos.message;
                mensajeDiv.style.color = "#4ade80";
                
            } catch (error) {
                console.error('Error al conectar:', error);
                mensajeDiv.innerText = 'Error: No se pudo conectar al servidor. Revisa que el puerto sea público.';
                mensajeDiv.style.color = "#f87171";
            }
        }

        document.getElementById('btn-conectar').addEventListener('click', conectarAPI);
    </script>
</body>
</html>
</file>

</files>
